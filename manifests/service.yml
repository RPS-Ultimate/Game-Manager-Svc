apiVersion: v1
kind: Service
metadata:
    name: "rpsbattle-game-manager"
    labels:
        app: "rpsbattle-game-manager"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "rpsbattle-game-manager"