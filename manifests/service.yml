apiVersion: v1
kind: Service
metadata:
    name: "rpsbattle-gamemanager"
    labels:
        app: "rpsbattle-gamemanager"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "rpsbattle-gamemanager"